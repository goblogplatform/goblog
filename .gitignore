coverage.txt
articles.json
profiles.json

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# database
*.db

# binary:
goblog

# env files
.env

# uploads
www/uploads/*
!www/uploads/.keep

# wasm plugin binaries (installed at runtime; keep the directory itself)
plugins/wasm/*
!plugins/wasm/.gitkeep

# installed themes (installed at runtime; keep the directory itself)
themes/installed/*
!themes/installed/.gitkeep

.idea

# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/
